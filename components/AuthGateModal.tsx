'use client';
import { useState } from 'react';
type Props = { isOpen: boolean; onClose: () => void; onVerified: (data:any)=>void; trigger: string; };
const AGE_RANGES=['18-23','23-28','28-33','33-38','38-43','43-48','48-53','53+'];
const GENDERS=['Male','Female','Non-binary','Trans','Prefer not to say'];
const ORIENTATIONS=['Straight','Gay','Lesbian','Bisexual','Pansexual','Asexual','Queer','Prefer not to say'];
export default function AuthGateModal({isOpen,onClose,onVerified,trigger}:Props){
  const [step,setStep]=useState(1);
  const [form,setForm]=useState({username:'',ageRange:'23-28',gender:'Male',orientation:'Straight',phone:'',email:'',password:'',confirmPassword:'',otp:'',paymentMethod:'card',agree:false});
  const [sent,setSent]=useState(false); const [real,setReal]=useState('');
  if(!isOpen) return null;
  const sendOtp=()=>{ const code=Math.floor(100000+Math.random()*900000).toString(); setReal(code); setSent(true); alert(`[DEV] OTP ${code} - use 123456 to bypass`); console.log('OTP',code); };
  const verify=()=>{ 
    if(form.password.length<6) return alert('Password must be 6+ chars - you typed '+form.password.length); 
    if(form.password!==form.confirmPassword) return alert('Passwords do not match - retype same'); 
    if(form.otp!==real && form.otp!=='123456') return alert('Wrong OTP - use 123456'); 
    if(!form.agree) return alert('Tick agree disclaimer'); 
    if(typeof window!=='undefined'){localStorage.setItem('buddy_verified_phone',form.phone); localStorage.setItem('buddy_user',JSON.stringify(form));} 
    onVerified(form); onClose(); 
  };
  return (<div className="fixed inset-0 z-[100] bg-black/80 backdrop-blur-md flex items-center justify-center p-4"><div className="w-full max-w-lg bg-[#111] border border-zinc-800 rounded-[32px] p-8 max-h-[90vh] overflow-y-auto"><div className="flex justify-between items-center"><h2 className="text-2xl font-black tracking-tight">JOIN BUDDY BLIND</h2><button onClick={onClose} className="w-8 h-8 rounded-full bg-zinc-800 flex items-center justify-center">✕</button></div><div className="mt-2 text-[11px] tracking-widest text-zinc-500">STEP {step} OF 3 • {trigger.toUpperCase()} • OTP 123456 WORKS</div>{step===1 && (<div className="mt-6 space-y-4"><div className="text-[11px] tracking-widest text-amber-300">BASIC INFO - Features Bible</div><input value={form.username} onChange={e=>setForm({...form,username:e.target.value})} placeholder="Username (nickname ok)" className="w-full h-12 rounded-full bg-black border border-zinc-800 px-6 text-sm outline-none focus:border-zinc-600"/><div className="grid grid-cols-2 gap-3"><select value={form.ageRange} onChange={e=>setForm({...form,ageRange:e.target.value})} className="h-12 rounded-full bg-black border border-zinc-800 px-6 text-sm">{AGE_RANGES.map(a=><option key={a} value={a}>{a}</option>)}</select><select value={form.gender} onChange={e=>setForm({...form,gender:e.target.value})} className="h-12 rounded-full bg-black border border-zinc-800 px-6 text-sm">{GENDERS.map(g=><option key={g} value={g}>{g}</option>)}</select></div><select value={form.orientation} onChange={e=>setForm({...form,orientation:e.target.value})} className="w-full h-12 rounded-full bg-black border border-zinc-800 px-6 text-sm">{ORIENTATIONS.map(o=><option key={o} value={o}>{o}</option>)}</select><input value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="Email" type="email" className="w-full h-12 rounded-full bg-black border border-zinc-800 px-6 text-sm outline-none"/><div className="flex gap-2"><input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})} placeholder="Phone for OTP +852" className="flex-1 h-12 rounded-full bg-black border border-zinc-800 px-6 text-sm outline-none"/><button onClick={sendOtp} className="px-6 h-12 rounded-full bg-white text-black font-black text-[11px] tracking-widest">SEND OTP</button></div>{sent && <div className="text-[11px] text-amber-300">✓ OTP sent - check alert. Use 123456 to bypass.</div>}<button disabled={!form.username||!form.email||!form.phone} onClick={()=>setStep(2)} className="w-full h-12 rounded-full bg-white text-black font-black text-xs tracking-widest disabled:opacity-30">NEXT → PASSWORD</button></div>)}{step===2 && (<div className="mt-6 space-y-4"><div className="text-[11px] tracking-widest text-amber-300">CREATE PASSWORD - 6+ chars, confirm retype</div><input value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="Create password (min 6 chars)" type="password" className="w-full h-12 rounded-full bg-black border border-zinc-800 px-6 text-sm outline-none"/><input value={form.confirmPassword} onChange={e=>setForm({...form,confirmPassword:e.target.value})} placeholder="Confirm - retype same password" type="password" className="w-full h-12 rounded-full bg-black border border-zinc-800 px-6 text-sm outline-none"/><p className="text-[11px] text-zinc-600">Length: {form.password.length} / 6 min. Match: {form.password===form.confirmPassword ? '✓' : '✗'}</p><div className="flex gap-3"><button onClick={()=>setStep(1)} className="flex-1 h-12 rounded-full border border-zinc-700 text-white font-black text-xs">BACK</button><button onClick={()=>{ if(form.password.length<6) return alert('Min 6 chars'); if(form.password!==form.confirmPassword) return alert('Mismatch'); setStep(3); }} className="flex-1 h-12 rounded-full bg-white text-black font-black text-xs">NEXT → VERIFY</button></div></div>)}{step===3 && (<div className="mt-6 space-y-4"><div className="text-[11px] tracking-widest text-amber-300">OTP + FREE TRIAL + DISCLAIMER</div><input value={form.otp} onChange={e=>setForm({...form,otp:e.target.value})} placeholder="Enter OTP - type 123456" className="w-full h-12 rounded-full bg-black border border-amber-400/20 px-6 text-sm text-center tracking-[0.3em] outline-none"/><div className="grid grid-cols-3 gap-2">{['card','apple_pay','google_pay'].map(m=><button key={m} onClick={()=>setForm({...form,paymentMethod:m})} className={`h-12 rounded-full border text-[11px] font-bold tracking-widest ${form.paymentMethod===m?'bg-white text-black border-white':'bg-black text-zinc-500 border-zinc-800'}`}>{m.replace('_',' ').toUpperCase()}</button>)}</div><div className="bg-black border border-zinc-800 rounded-2xl p-4 text-[11px] text-zinc-500 leading-relaxed"><b className="text-white">HEART PREMIUM 90-DAY FREE:</b> No charge today. After trial $50/mo. Cancel anytime in settings. $5 admin per booking, $88 deposit at venue. Blind dining: address 2h before, no menu, photos optional, respectful. 18+ only.</div><label className="flex gap-3 items-start text-[11px] text-zinc-400"><input type="checkbox" checked={form.agree} onChange={e=>setForm({...form,agree:e.target.checked})} className="mt-1"/><span>I agree to disclaimer, 18+, OTP verified, respectful community, no harassment.</span></label><div className="flex gap-3"><button onClick={()=>setStep(2)} className="flex-1 h-12 rounded-full border border-zinc-700 text-white font-black text-xs">BACK</button><button onClick={verify} className="flex-1 h-12 rounded-full bg-amber-400 text-black font-black text-xs">VERIFY & START →</button></div></div>)}</div></div>);
}
