import { NextRequest, NextResponse } from 'next/server'; import Stripe from 'stripe'; export async function POST(req: NextRequest){ const secret=process.env.STRIPE_SECRET_KEY; if(!secret) return NextResponse.json({error:'Missing key'},{status:500}); const stripe=new Stripe(secret,{apiVersion:'2024-06-20' as any}); const body=await req.json().catch(()=>({})); const intent=await stripe.paymentIntents.create({amount:body.amount||4500,currency:body.currency||'nzd',automatic_payment_methods:{enabled:true}}); return NextResponse.json({clientSecret:intent.client_secret}); }