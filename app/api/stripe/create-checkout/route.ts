import { NextRequest, NextResponse } from 'next/server'; import Stripe from 'stripe'; export async function POST(req: NextRequest){ const secret=process.env.STRIPE_SECRET_KEY; if(!secret) return NextResponse.json({error:'Missing key'},{status:500}); const stripe=new Stripe(secret,{apiVersion:'2024-06-20' as any}); const body=await req.json().catch(()=>({})); const session=await stripe.checkout.sessions.create({payment_method_types:['card'],line_items:body.line_items||[{price_data:{currency:'nzd',product_data:{name:'Buddy Blind'},unit_amount:4500},quantity:1}],mode:'payment',success_url:body.success_url||'http://localhost:3000/success',cancel_url:body.cancel_url||'http://localhost:3000/cancel'}); return NextResponse.json({url:session.url}); }