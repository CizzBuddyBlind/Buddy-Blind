'use client'; import {useEffect,useState} from 'react'; import {supabase} from '@/lib/supabase'; import VenueCard from '@/components/VenueCard'; export default function Home(){ const [venues,setVenues]=useState<any[]>([]); useEffect(()=>{ supabase.from('venues').select('*').then(({data})=>{ if(data) setVenues(data); }); },[]); return (<div className='min-h-screen bg-[#050505] text-white px-6 py-20 max-w-6xl mx-auto'><h1 className='text-5xl font-black'>BUDDY BLIND</h1><p className='mt-4 text-zinc-500'>Browse free. JOIN / INVITE requires OTP verification.</p><div className='mt-12 grid md:grid-cols-3 gap-6'>{venues.map(v=><VenueCard key={v.id} venue={v} />)}</div></div>); }