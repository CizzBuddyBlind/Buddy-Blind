'use client'; import {useEffect,useState} from 'react'; import {supabase} from '@/lib/supabase'; import VenueCard from '@/components/VenueCard'; export default function Home(){ const [venues,setVenues]=useState<any[]>([]); useEffect(()=>{ supabase.from('venues').select('*').limit(12).then(({data})=>{ if(data) setVenues(data); }); },[]); return (<div className='min-h-screen bg-[#050505] text-white'><div className='max-w-6xl mx-auto px-6 py-20'><h1 className='text-5xl md:text-7xl font-black tracking-tight'>BUDDY BLIND</h1><p className='mt-4 text-zinc-500 max-w-xl'>Browse venues free. JOIN / INVITE requires OTP verification per Features Bible. Username, Age 18-23...53+, Gender, Orientation, Phone OTP, Email, Password 6+ chars.</p><div className='mt-12 grid md:grid-cols-3 gap-6'>{venues.map(v=><VenueCard key={v.id} venue={v} />)}{venues.length===0 && <div className='col-span-3 p-12 rounded-[28px] bg-[#111] border border-zinc-800 text-zinc-500'>No venues yet - add in Supabase.</div>}</div></div></div>); }