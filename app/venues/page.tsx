'use client'; import {useEffect,useState} from 'react'; import {supabase} from '@/lib/supabase'; import VenueCard from '@/components/VenueCard'; export default function Venues(){ const [venues,setVenues]=useState<any[]>([]); useEffect(()=>{ supabase.from('venues').select('*').then(({data})=>{ if(data) setVenues(data); }); },[]); return (<div className='min-h-screen bg-[#050505] text-white px-6 py-20 max-w-6xl mx-auto'><h1 className='text-5xl font-black'>VENUES</h1><div className='mt-12 grid md:grid-cols-3 gap-6'>{venues.map(v=><VenueCard key={v.id} venue={v} />)}</div></div>); }