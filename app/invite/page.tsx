'use client'
import { useState } from 'react'
export default function Invite(){const [copied,setCopied]=useState(false); const link='https://buddy-blind.vercel.app/join?ref=cizz-HEART'; return(<main className="max-w-2xl mx-auto px-6 py-20 text-center"><h1 className="serif text-[48px]">INVITE</h1><p className="mono text-[11px] text-zinc-500 mt-2">Good taste is scarce. 3 invites per month.</p><div className="mt-10 bg-[#111] border border-zinc-800 rounded-[24px] p-6"><div className="mono text-[10px] text-zinc-500">YOUR LINK</div><div className="mt-3 bg-black border border-zinc-800 rounded-full px-4 py-3 text-xs font-mono truncate">{link}</div><button onClick={()=>{navigator.clipboard.writeText(link); setCopied(true); setTimeout(()=>setCopied(false),2000)}} className="mt-4 w-full h-12 rounded-full bg-white text-black mono text-[11px]">{copied?'COPIED':'COPY LINK'}</button></div></main>)}