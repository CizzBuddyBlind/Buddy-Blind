'use client';
import { useState } from 'react';
type Props = { isOpen: boolean; onClose: () => void; onVerified: (data:any)=>void; trigger: 'invite'|'join'|'premium'|'host'; };
const AGE_RANGES=['18-23','23-28','28-33','33-38','38-43','43-48','48-53','53+'];
const GENDERS=['Male','Female','Non-binary','Trans','Prefer not to say'];
const ORIENTATIONS=['Straight','Gay','Lesbian','Bisexual','Pansexual','Asexual','Queer','Prefer not to say'];
export default function AuthGateModal({isOpen,onClose,onVerified,trigger}:Props){
  const [step,setStep]=useState(1);
  const [form,setForm]=useState({username:'',ageRange:'23-28',gender:'Male',orientation:'Straight',phone:'',email:'',password:'',confirmPassword:'',otp:'',paymentMethod:'card',agree:false});
  const [otpSent,setOtpSent]=useState(false); const [realOtp,setRealOtp]=useState('');
  if(!isOpen) return null;
  const sendOtp=()=>{ const code=Math.floor(100000+Math.random()*900000).toString(); setRealOtp(code); setOtpSent(true); alert(`[DEV OTP] for ${form.phone}: ${code} - use 123456 also works`); };
  const verify=()=>{ if(form.otp!==realOtp && form.otp!=='123456') return alert('Wrong OTP - try 123456'); if(!form.agree) return alert('Agree to disclaimer'); if(form.password!==form.confirmPassword) return alert('Passwords mismatch'); if(form.password.length<6) return alert('Min 6 chars'); localStorage.setItem('buddy_verified_phone',form.phone); localStorage.setItem('buddy_user',JSON.stringify(form)); onVerified(form); onClose(); };
  return (<div className="fixed inset-0 z-[100] bg-black/80 backdrop-blur-md flex items-center justify-center p-4"><div className="w-full max-w-lg bg-[#111] border border-zinc-800 rounded-[32px] p-8 max-h-[90vh] overflow-y-auto"><div className="flex justify-between items-center"><h2 className="text-2xl font-black">JOIN BUDDY BLIND</h2><button onClick={onClose} className="w-8 h-8 rounded-full bg-zinc-800 flex items-center justify-center">✕</button></div><div className="mt-2 text-[11px] tracking-widest text-zinc-500">STEP {step} OF 3 • {trigger.toUpperCase()}</div>{step===1 && (<div className="mt-6 space-y-4"><div className="text-[11px] tracking-widest text-amber-300">BASIC INFO</div><input value={form.username} onChange={e=>setForm({...form,username:e.target.value})} placeholder="Username" className="w-full h-12 rounded-full bg-black border border-zinc-800 px-6 text-sm"/><div className="grid grid-cols-2 gap-3"><select value={form.ageRange} onChange={e=>setForm({...form,ageRange:e.target.value})} className="h-12 rounded-full bg-black border border-zinc-800 px-6 text-sm">{AGE_RANGES.map(a=><option key={a} value={a}>{a}</option>)}</select><select value={form.gender} onChange={e=>setForm({...form,gender:e.target.value})} className="h-12 rounded-full bg-black border border-zinc-800 px-6 text-sm">{GENDERS.map(g=><option key={g} value={g}>{g}</option>)}</select></div><select value={form.orientation} onChange={e=>setForm({...form,orientation:e.target.value})} className="w-full h-12 rounded-full bg-black border border-zinc-800 px-6 text-sm">{ORIENTATIONS.map(o=><option key={o} value={o}>{o}</option>)}</select><input value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="Email" className="w-full h-12 rounded-full bg-black border border-zinc-800 px-6 text-sm"/><div className="flex gap-2"><input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})} placeholder="Phone for OTP" className="flex-1 h-12 rounded-full bg-black border border-zinc-800 px-6 text-sm"/><button onClick={sendOtp} className="px-6 h-12 rounded-full bg-white text-black font-black text-[11px]">SEND OTP</button></div>{otpSent && <div className="text-[11px] text-amber-300">OTP sent - check alert</div>}<button disabled={!form.username||!form.email||!form.phone} onClick={()=>setStep(2)} className="w-full h-12 rounded-full bg-white text-black font-black text-xs disabled:opacity-30">NEXT → PASSWORD</button></div>)}{step===2 && (<div className="mt-6 space-y-4"><div className="text-[11px] tracking-widest text-amber-300">PASSWORD</div><input value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="Create password" type="password" className="w-full h-12 rounded-full bg-black border border-zinc-800 px-6 text-sm"/><input value={form.confirmPassword} onChange={e=>setForm({...form,confirmPassword:e.target.value})} placeholder="Confirm - retype" type="password" className="w-full h-12 rounded-full bg-black border border-zinc-800 px-6 text-sm"/><div className="flex gap-3"><button onClick={()=>setStep(1)} className="flex-1 h-12 rounded-full border border-zinc-700 text-white font-black text-xs">BACK</button><button onClick={()=>{ if(form.password!==form.confirmPassword) return alert('Mismatch'); if(form.password.length<6) return alert('Min 6'); setStep(3); }} className="flex-1 h-12 rounded-full bg-white text-black font-black text-xs">NEXT → VERIFY</button></div></div>)}{step===3 && (<div className="mt-6 space-y-4"><div className="text-[11px] tracking-widest text-amber-300">OTP + TRIAL + DISCLAIMER</div><input value={form.otp} onChange={e=>setForm({...form,otp:e.target.value})} placeholder="Enter OTP 123456" className="w-full h-12 rounded-full bg-black border border-zinc-800 px-6 text-sm text-center tracking-widest"/><div className="grid grid-cols-3 gap-2">{['card','apple_pay','google_pay'].map(m=><button key={m} onClick={()=>setForm({...form,paymentMethod:m})} className={`h-12 rounded-full border text-[11px] font-bold ${form.paymentMethod===m?'bg-white text-black':'bg-black text-zinc-500 border-zinc-800'}`}>{m.toUpperCase()}</button>)}</div><div className="bg-black border border-zinc-800 rounded-2xl p-4 text-[11px] text-zinc-500 leading-relaxed"><b className="text-white">HEART PREMIUM 90-DAY FREE:</b> No charge today. After trial $50/mo. Cancel anytime. $5 admin per book, $88 deposit at venue. Address revealed 2h before, no menu, respectful behavior. 18+ only.</div><label className="flex gap-3 text-[11px] text-zinc-400"><input type="checkbox" checked={form.agree} onChange={e=>setForm({...form,agree:e.target.checked})}/><span>I agree to disclaimer, 18+, OTP verified, respectful community.</span></label><div className="flex gap-3"><button onClick={()=>setStep(2)} className="flex-1 h-12 rounded-full border border-zinc-700 text-white font-black text-xs">BACK</button><button onClick={verify} className="flex-1 h-12 rounded-full bg-amber-400 text-black font-black text-xs">VERIFY & START</button></div></div>)}</div></div>);
}
